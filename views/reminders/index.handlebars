{{#each reminders}}
<div class="card card-body mb-2">
    <h4>{{event_title}}</h4>
    <p>{{details}}</p>
    <a href="/reminders/edit/{{id}}" class="btn btn-dark btn-block">Edit</a>
</div>
{{else}}
<p>No Reminders Recently</p>
{{/each}}